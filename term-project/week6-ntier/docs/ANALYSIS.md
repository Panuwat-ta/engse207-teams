# 📊 การวิเคราะห์ระบบกระดานงาน (Task Board System Analysis)

เอกสารนี้จะนำเสนอการวิเคราะห์เบื้องต้นสำหรับระบบกระดานงาน โดยมุ่งเน้นไปที่การทำความเข้าใจความต้องการ เหตุผลในการเลือกสถาปัตยกรรม และการวิเคราะห์ส่วนประกอบหลักของระบบ

---

## 1. คำแถลงปัญหาและความต้องการ (Problem Statement & Requirements)

### 1.1 ปัญหา
ในสภาพแวดล้อมการทำงานร่วมกัน การติดตามงานและสถานะความคืบหน้าเป็นสิ่งสำคัญ ทีมต้องการวิธีที่ง่ายและมีประสิทธิภาพในการจัดการงานต่างๆ เพื่อให้ทุกคนสามารถมองเห็นภาพรวมของสิ่งที่ต้องทำและสถานะปัจจุบันของงานได้อย่างชัดเจน

### 1.2 ความต้องการหลัก (Core Requirements)
ระบบกระดานงานควรมีความสามารถดังต่อไปนี้:
-   **การจัดการงาน (Task Management):**
    -   สามารถเพิ่มงานใหม่ได้
    -   สามารถดูรายการงานทั้งหมดได้
    -   สามารถทำเครื่องหมายงานว่าเสร็จสมบูรณ์ได้
    -   สามารถลบงานออกจากระบบได้
-   **ส่วนติดต่อผู้ใช้ (User Interface):**
    -   มีหน้าจอที่ใช้งานง่ายสำหรับการแสดงและจัดการงาน
-   **ความน่าเชื่อถือ (Reliability):**
    -   ข้อมูลงานควรได้รับการจัดเก็บอย่างถาวร
-   **ความยืดหยุ่น (Flexibility):**
    -   สถาปัตยกรรมควรเอื้อต่อการขยายและการปรับเปลี่ยนในอนาคต

---

## 2. เหตุผลในการเลือกสถาปัตยกรรม (Architectural Choices Justification)

### 2.1 สถาปัตยกรรมแบบ N-Tier
เลือกใช้สถาปัตยกรรมแบบ N-Tier (3-Tier) เพื่อแยกส่วนประกอบของระบบออกเป็นชั้นที่ชัดเจน ซึ่งให้ประโยชน์ดังนี้:
-   **การแยกความรับผิดชอบ (Separation of Concerns):** แต่ละชั้นมีความรับผิดชอบเฉพาะ ทำให้เข้าใจและจัดการได้ง่ายขึ้น
-   **ความสามารถในการปรับขนาด (Scalability):** แต่ละชั้นสามารถปรับขนาดได้อย่างอิสระตามความต้องการ
-   **ความปลอดภัย (Security):** สามารถใช้มาตรการรักษาความปลอดภัยที่แตกต่างกันในแต่ละชั้น
-   **ความยืดหยุ่นทางเทคโนโลยี (Technology Flexibility):** สามารถเปลี่ยนเทคโนโลยีในชั้นใดชั้นหนึ่งได้โดยไม่ส่งผลกระทบต่อชั้นอื่นมากนัก

### 2.2 เทคโนโลยีที่เลือกใช้
-   **Web Server (Nginx):**
    -   เป็นเว็บเซิร์ฟเวอร์ที่มีประสิทธิภาพสูง เสถียร และใช้ทรัพยากรน้อย เหมาะสำหรับการให้บริการไฟล์ static และทำ Reverse Proxy
-   **Application Server (Node.js & Express.js):**
    -   Node.js เป็น Runtime ที่เหมาะสำหรับการพัฒนา API ที่ต้องการความเร็วและรองรับการทำงานแบบ Asynchronous
    -   Express.js เป็นเฟรมเวิร์ก Node.js ที่นิยมสำหรับการสร้าง RESTful API ที่รวดเร็ว
-   **Database Server (PostgreSQL):**
    -   เป็นระบบจัดการฐานข้อมูลเชิงสัมพันธ์ (RDBMS) ที่มีประสิทธิภาพ เสถียร และเป็นที่นิยม เหมาะสำหรับการจัดเก็บข้อมูลที่มีโครงสร้าง

---

## 3. การวิเคราะห์ส่วนประกอบ (Component Analysis)

### 3.1 เว็บเซิร์ฟเวอร์ (Nginx - VM1)
-   **หน้าที่:**
    -   ให้บริการไฟล์ HTML, CSS, JavaScript ของส่วนหน้า (Frontend)
    -   ทำหน้าที่เป็น Reverse Proxy เพื่อส่งต่อคำขอ API ไปยัง Application Server
-   **การเชื่อมต่อ:** รับคำขอจากผู้ใช้ผ่านพอร์ต 443 และส่งต่อคำขอ API ไปยัง VM2

### 3.2 แอปพลิเคชันเซิร์ฟเวอร์ (Node.js/Express.js - VM2)
-   **หน้าที่:**
    -   มีตรรกะทางธุรกิจ (Business Logic) สำหรับการจัดการงาน
    -   เปิดเผย RESTful API (Create, Read, Update, Delete) สำหรับการโต้ตอบกับข้อมูลงาน
-   **การเชื่อมต่อ:** รับคำขอ API จาก Nginx (VM1) และเชื่อมต่อกับ Database Server (VM3) เพื่อดำเนินการกับข้อมูล ผ่านพอร์ต 3000

### 3.3 ฐานข้อมูลเซิร์ฟเวอร์ (PostgreSQL  - VM3)
-   **หน้าที่:**
    -   จัดเก็บข้อมูลงานทั้งหมดอย่างถาวรในตาราง `tasks` ภายในฐานข้อมูล `taskboard`
-   **การเชื่อมต่อ:** รับคำขอจาก Application Server (VM2) เพื่อบันทึก ดึงข้อมูล อัปเดต และลบข้อมูลงาน ผ่านพอร์ต 5432

---

## 4. ข้อควรพิจารณาและข้อแลกเปลี่ยน (Considerations and Trade-offs)

-   **ความซับซ้อน (Complexity):** การใช้สถาปัตยกรรมแบบ N-Tier ทำให้ระบบมีความซับซ้อนในการตั้งค่าและดูแลมากกว่าระบบ Monolithic แบบง่ายๆ
-   **ประสิทธิภาพ (Performance):** การแยกชั้นออกเป็น VM ที่ต่างกันอาจเพิ่ม Latency ในการสื่อสารระหว่างชั้นเล็กน้อย แต่แลกมาด้วยความสามารถในการปรับขนาดที่ดีขึ้น
-   **การบำรุงรักษา (Maintainability):** การแยกส่วนประกอบทำให้ง่ายต่อการบำรุงรักษาและแก้ไขปัญหาในแต่ละส่วน แต่ต้องมีการจัดการการสื่อสารระหว่างส่วนประกอบให้ดี
-   **ค่าใช้จ่าย (Cost):** การใช้หลาย VM อาจเพิ่มการใช้ทรัพยากร (CPU, RAM) และค่าใช้จ่ายในการโฮสต์ในสภาพแวดล้อมจริง
