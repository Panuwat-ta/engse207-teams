# สถาปัตยกรรมแบบ N-Tier

โปรเจกต์นี้ใช้สถาปัตยกรรมแบบ **3-Tier** แบบคลาสสิก โดยใช้ **Vagrant** ในการจัดเตรียมเครื่องเสมือนแยกกัน 3 เครื่อง ซึ่งแต่ละเครื่องแทนหนึ่งชั้นของระบบอย่างชัดเจน

* **Tier 1: เว็บเซิร์ฟเวอร์ (VM1)** — Nginx
* **Tier 2: แอปพลิเคชันเซิร์ฟเวอร์ (VM2)** — Node.js & Express.js
* **Tier 3: ฐานข้อมูลเซิร์ฟเวอร์ (VM3)** — PostgreSQL

## แผนภาพสถาปัตยกรรมระบบ

![สถาปัตยกรรมระบบ](../img/vagrant.png)

---

## Tier 1: เว็บเซิร์ฟเวอร์ (Nginx)

* **VM:** `vm1`
* **ส่วนประกอบ:** Nginx
* **วัตถุประสงค์:**

  * ให้บริการไฟล์ส่วนหน้าแบบคงที่ (HTML, CSS, JavaScript)
  * ทำหน้าที่เป็น **Reverse Proxy** ส่งต่อคำขอ API ไปยัง Tier 2
* **พอร์ตที่ให้บริการ:**

  * **80 (HTTP)** / **443 (HTTPS)** บน VM

เซิร์ฟเวอร์ Nginx ถูกกำหนดค่าให้ให้บริการไฟล์จากไดเรกทอรี `/public` และส่งต่อคำขอที่ขึ้นต้นด้วย `/api` ไปยังแอปพลิเคชันเซิร์ฟเวอร์ (Tier 2)
![](../img/vm1-port.png)

---

## Tier 2: แอปพลิเคชันเซิร์ฟเวอร์ (Node.js)

* **VM:** `vm2`
* **ส่วนประกอบ:** Node.js พร้อม Express.js
* **วัตถุประสงค์:**

  * ประมวลผลตรรกะทางธุรกิจหลัก (Business Logic)
  * เปิดเผย **RESTful API** สำหรับการจัดการงาน (CRUD)
* **พอร์ตที่ให้บริการ:**

  * **3000** บน VM

แอปพลิเคชันเซิร์ฟเวอร์รับคำขอจาก Nginx และเชื่อมต่อกับฐานข้อมูลใน Tier 3 เพื่อดำเนินการ Create, Read, Update และ Delete ข้อมูล
![](../img/vm2-port.png)
---

## Tier 3: ฐานข้อมูลเซิร์ฟเวอร์ (PostgreSQL)

* **VM:** `vm3`
* **ส่วนประกอบ:** PostgreSQL
* **วัตถุประสงค์:**

  * จัดเก็บข้อมูลงานของแอปพลิเคชันอย่างถาวร
* **พอร์ตที่ให้บริการ:**

  * **5432** (PostgreSQL)

ฐานข้อมูลถูกเริ่มต้นด้วยฐานข้อมูลชื่อ `taskboard_db` และมีตาราง `tasks` สำหรับจัดเก็บข้อมูลงาน
![](../img/vm3-port.png)
---

## การสื่อสารเครือข่าย (Network Communication)

* **Client (Web Browser)** → **Nginx (VM1)** ผ่าน HTTP/HTTPS (80/443)
* **Nginx (VM1)** → **Node.js API (VM2)** ผ่าน HTTP (3000)
* **Node.js (VM2)** → **PostgreSQL (VM3)** ผ่าน TCP (5432)

## 🔄 Request Flow

1. ผู้ใช้ส่งคำขอ HTTP/HTTPS จากเว็บเบราว์เซอร์ไปยัง Nginx (VM1)
2. Nginx ทำหน้าที่เป็น Reverse Proxy ส่งต่อคำขอ API ไปยัง Node.js (VM2)
3. Node.js ติดต่อ PostgreSQL (VM3) เพื่อจัดการข้อมูล และส่งผลลัพธ์ย้อนกลับไปยังผู้ใช้
