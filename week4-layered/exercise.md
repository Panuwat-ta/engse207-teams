# 🎯 แบบฝึกหัด 1: Layer Decision Tree

คำถาม: โค้ดต่อไปนี้ควรอยู่ใน Layer ไหน? เพราะอะไร?

---

`1. const tasks = await database.all('SELECT * FROM tasks')`

- **คำตอบ:** **Repository**
- **เหตุผล:** เพราะเป็นการติดต่อกับฐานข้อมูลโดยตรงเพื่อดึงข้อมูลทั้งหมดจากตาราง `tasks` Layer **Repository** มีหน้าที่รับผิดชอบในการจัดการกับการเข้าถึงข้อมูล (Data Access)

---

`2. if (title.length < 3) throw new Error('Title too short')`

- **คำตอบ:** **Service**
- **เหตุผล:** เพราะเป็น Business Logic ที่ตรวจสอบความถูกต้องของข้อมูล (Validation) ตามกฎของระบบ เช่น ชื่อ Task ต้องยาวอย่างน้อย 3 ตัวอักษร Layer **Service** จะรับผิดชอบ Business Logic และ Validation ที่ซับซ้อน

---

`3. res.status(201).json({ success: true, data: task })`

- **คำตอบ:** **Controller**
- **เหตุผล:** เพราะเป็นการจัดการ HTTP Response ที่จะส่งกลับไปยัง Client Layer **Controller** มีหน้าที่รับ HTTP Request และส่ง HTTP Response กลับไป ซึ่งรวมถึงการกำหนด Status Code และข้อมูลที่จะส่ง

---

`4. if (priority === 'HIGH' && !description) throw new Error(...)`

- **คำตอบ:** **Service**
- **เหตุผล:** เพราะเป็น Business Logic ที่ซับซ้อนซึ่งขึ้นอยู่กับข้อมูลหลายฟิลด์ (Cross-field validation) เช่น ถ้า Priority เป็น `HIGH` จะต้องมี `description` ด้วยเสมอ

---

`5. const taskData = { title: req.body.title, description: req.body.description }`

- **คำตอบ:** **Controller**
- **เหตุผล:** เพราะเป็นการจัดการข้อมูลที่มาจาก HTTP Request (`req.body`) เพื่อเตรียมข้อมูลให้อยู่ในรูปแบบที่เหมาะสมก่อนที่จะส่งต่อไปให้ Layer **Service** ซึ่งเป็นหน้าที่หลักของ **Controller**

---
---

# ส่วนที่ 4: การวิเคราะห์และเอกสาร

## 🎯 ส่วนที่ 4.1: การวิเคราะห์เปรียบเทียบ

### คำถาม 1: การเปรียบเทียบโครงสร้าง


| ข้อมูล              | Monolithic (Week 3) | Layered (Week 4) |
| ------------------- | ------------------- | ---------------- |
| จำนวนไฟล์ JS หลัก  | 1                   | 9                |
| จำนวนบรรทัดทั้งหมด | 403    | 61     |
| จำนวน layers        | 1                   | 6 |
| ความซับซ้อนโดยรวม   | ต่ำถึงปานกลาง       | ปานกลางถึงสูง     |

**คำถาม:**
- Layered มีจำนวนไฟล์และบรรทัดโค้ดมากกว่าหรือน้อยกว่า Monolithic? เพราะอะไร?
- ความซับซ้อนที่เพิ่มขึ้นคุ้มค่าหรือไม่? อธิบาย

**คำตอบของคุณ:**

**Layered** architecture มีจำนวนไฟล์มากกว่า **Monolithic** อย่างเห็นได้ชัด เพราะมีการแบ่งโค้ดออกเป็นส่วนๆ ตามหน้าที่ความรับผิดชอบ (Separation of Concerns) ทำให้แต่ละไฟล์มีขนาดเล็กลงและมุ่งเน้นการทำงานเพียงอย่างเดียว จำนวนบรรทัดโค้ดโดยรวมอาจจะมากกว่าเล็กน้อยเนื่องจากมี Boilerplate code สำหรับการเชื่อมต่อระหว่าง Layer เพิ่มขึ้น

ความซับซ้อนที่เพิ่มขึ้นนั้นคุ้มค่าสำหรับโปรเจกต์ขนาดกลางถึงใหญ่ หรือโปรเจกต์ที่คาดว่าจะมีการพัฒนาต่อในระยะยาว เพราะถึงแม้จะมีความซับซ้อนในการตั้งค่าเริ่มต้น แต่จะช่วยให้การดูแลรักษา (Maintainability), การทดสอบ (Testability), และการขยายระบบ (Scalability) ทำได้ง่ายขึ้นมากในอนาคต

---

### คำถาม 2: จุดแข็ง-จุดอ่อน

**วิเคราะห์จุดแข็งของ Layered Architecture:**

| Quality Attribute     | Monolithic | Layered | คะแนน (1-5) | อธิบายเหตุผล                                                                                                                                                               |
| --------------------- | ---------- | ------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Maintainability**   | 2          | 4       | **Layered: 4**  | **Layered** แบ่งโค้ดชัดเจน ทำให้แก้บั๊กหรือเพิ่มฟีเจอร์ง่ายกว่าโดยกระทบส่วนอื่นน้อยลง **Monolithic** โค้ดรวมกันหมด แก้ไขจุดหนึ่งอาจกระทบหลายส่วน                                |
| **Testability**       | 2          | 4       | **Layered: 4**  | **Layered** สามารถทดสอบแต่ละ Layer แยกกันได้ (Unit Test) เช่น ทดสอบ Business Logic ใน **Service** โดยไม่ต้องเชื่อมต่อฐานข้อมูลจริง ในขณะที่ **Monolithic** ต้องทดสอบแบบ End-to-End ซึ่งซับซ้อนกว่า |
| **Modifiability**     | 2          | 4       | **Layered: 4**  | การเปลี่ยนแปลงใน Layer หนึ่ง เช่น การเปลี่ยนฐานข้อมูลใน **Repository** จะไม่กระทบกับ Business Logic ใน **Service** หรือ UI ใน **Controller**                                 |
| **Reusability**       | 1          | 3       | **Layered: 3**  | สามารถนำ **Service** หรือ **Repository** ไปใช้ในส่วนอื่นของแอปพลิเคชันได้ง่ายกว่า **Monolithic** ซึ่งทุกอย่างผูกติดกัน                                                                      |
| **Team Collaboration**| 2          | 5       | **Layered: 5**  | ทีมสามารถแบ่งกันทำงานตาม Layer ได้ เช่น Frontend Team ทำงานกับ **Controller**, Backend Team ทำงานกับ **Service** และ DBA ทำงานกับ **Repository** โดยไม่ก้าวก่ายกัน                         |
| **Performance**       | 4          | 3       | **Layered: 3**  | **Monolithic** อาจเร็วกว่าเล็กน้อยเพราะไม่มี Overhead จากการเรียกฟังก์ชันข้าม Layer แต่ในแอปพลิเคชันส่วนใหญ่ความแตกต่างนี้ไม่มีนัยสำคัญ                                           |
| **Simplicity**        | 5          | 3       | **Layered: 3**  | **Monolithic** ง่ายกว่าในการเริ่มต้นและสำหรับโปรเจกต์ขนาดเล็ก แต่จะซับซ้อนขึ้นอย่างรวดเร็วเมื่อโปรเจกต์ใหญ่ขึ้น **Layered** ซับซ้อนกว่าในตอนแรก แต่จัดการง่ายกว่าในระยะยาว        |

---

### คำถาม 3: สถานการณ์จริง

**วิเคราะห์สถานการณ์ต่อไปนี้:**

**สถานการณ์ที่ 1: ต้องการเพิ่มฟีเจอร์ "assign task to user"**
- **ใน Monolithic จะต้องแก้ไขอย่างไร?**
  - ต้องแก้ไขไฟล์ `server.js` โดยตรง เพิ่ม Logic การดึงข้อมูล User, การอัปเดต Task และการจัดการ Route ทั้งหมดในที่เดียว
- **ใน Layered จะต้องแก้ไขอย่างไร?**
  - **Controller:** เพิ่ม `assignTaskToUser` endpoint ใหม่
  - **Service:** เพิ่ม `assignTaskToUser` method ที่มี Business Logic สำหรับการ assign
  - **Repository:** เพิ่ม `updateTaskUser` และ `findUserById` methods เพื่อจัดการข้อมูล
- **แบบไหนง่ายกว่า? เพราะอะไร?**
  - **Layered** ง่ายกว่าในแง่ของการจัดการโค้ด เพราะการเปลี่ยนแปลงถูกแบ่งเป็นส่วนๆชัดเจน ทำให้ตรวจสอบและทดสอบได้ง่าย

**สถานการณ์ที่ 2: มีบั๊กที่ validation logic (ตรวจสอบ title)**
- **ใน Monolithic จะต้องหาบั๊กและแก้ไขที่ไหน?**
  - ต้องค้นหาในไฟล์ `server.js` ที่เดียว ซึ่งอาจจะหาได้เร็วถ้าไฟล์ไม่ใหญ่มาก แต่ถ้าโค้ดซับซ้อนจะหายาก
- **ใน Layered จะต้องหาบั๊กและแก้ไขที่ไหน?**
  - สามารถหาบั๊กได้ที่ `taskService.js` หรือ `middleware/validator.js` ได้ทันที เพราะรู้ว่า Logic การตรวจสอบข้อมูลอยู่ที่ไหน
- **แบบไหนง่ายกว่า? เพราะอะไร?**
  - **Layered** ง่ายกว่าและเร็วกว่าในการหาและแก้บั๊ก เพราะมีโครงสร้างที่ชัดเจน

**สถานการณ์ที่ 3: ต้องการเปลี่ยนจาก SQLite เป็น PostgreSQL**
- **ใน Monolithic จะต้องแก้ไขกี่ที่?**
  - ต้องแก้ไขทุกส่วนของโค้ดที่เรียกใช้ `database.all`, `database.run` หรือคำสั่ง SQL ที่เกี่ยวกับ SQLite ซึ่งกระจายอยู่ทั่วทั้งไฟล์
- **ใน Layered จะต้องแก้ไขกี่ที่?**
  - แก้ไขเฉพาะใน `taskRepository.js` และไฟล์ `connection.js` เท่านั้น Layer อื่นๆ (**Service**, **Controller**) ไม่ต้องแก้ไขเลย
- **แบบไหนง่ายกว่า? เพราะอะไร?**
  - **Layered** ง่ายกว่าอย่างยิ่ง เพราะการเปลี่ยนแปลงถูกจำกัดอยู่ใน Layer เดียว (**Repository**) ซึ่งเป็น Abstraction Layer สำหรับการเข้าถึงข้อมูล

---

### คำถาม 4: Trade-offs

**วิเคราะห์ Trade-offs:**

**Complexity vs Maintainability**
- **Layered มีความซับซ้อนมากขึ้น แต่ดูแลง่ายขึ้น คุณคิดว่า trade-off นี้คุ้มค่าหรือไม่? เพราะอะไร?**
  - คุ้มค่าอย่างยิ่งสำหรับโปรเจกต์ที่ไม่ได้มีขนาดเล็กมาก เพราะความซับซ้อนที่เพิ่มขึ้นในช่วงแรกจะถูกชดเชยด้วยความสามารถในการบำรุงรักษาที่ง่ายขึ้นในระยะยาว ช่วยลดต้นทุนและเวลาในการพัฒนาในอนาคต
- **ในกรณีไหนที่คุ้มค่า? ในกรณีไหนที่ไม่คุ้มค่า?**
  - **คุ้มค่า:** โปรเจกต์ขนาดกลางถึงใหญ่, ทีมพัฒนาหลายคน, โปรเจกต์ที่ต้องการการบำรุงรักษาระยะยาว, โปรเจกต์ที่มี Business Logic ซับซ้อน
  - **ไม่คุ้มค่า:** โปรเจกต์ขนาดเล็กมาก (เช่น script, prototype), ทีมพัฒนาคนเดียว, โปรเจกต์ที่ทำเสร็จแล้วทิ้ง (เช่น งานสำหรับ event ครั้งเดียว)

**Performance Overhead**
- **Layered มี overhead จากการเรียกผ่าน layers คุณคิดว่ามีผลกระทบมากแค่ไหน?**
  - ในแอปพลิเคชันส่วนใหญ่ (เช่น Web Application ทั่วไป) ผลกระทบมีน้อยมากจนแทบวัดไม่ได้ Overhead จากการเรียกฟังก์ชันข้าม Layer นั้นน้อยกว่าเวลาที่ใช้ในการ I/O (เช่น การเรียกฐานข้อมูล, การเรียก API ภายนอก) อย่างมหาศาล
- **ในแอปพลิเคชันประเภทใดที่ performance overhead นี้สำคัญ?**
  - อาจจะสำคัญในแอปพลิเคชันที่ต้องการ Latency ต่ำมากๆ (Low-latency) และมี High-throughput สูง เช่น ระบบ Real-time bidding, High-frequency trading หรือ Game Server ที่ทุกๆ millisecond มีความสำคัญ

---

### คำถาม 5: การตัดสินใจเลือกใช้

**ออกแบบกฎการตัดสินใจ:**

**Decision Tree สำหรับตัดสินใจว่าจะใช้ Monolithic หรือ Layered:**
```
เริ่มต้นโปรเจกต์
│
├─ ขนาดทีม?
│  ├─ 1-2 คน → Monolithic (ถ้าโปรเจกต์เล็ก) หรือ Layered (ถ้าคาดว่าจะขยายทีม)
│  └─ 3+ คน → Layered
│
├─ ขนาดโปรเจกต์?
│  ├─ เล็ก (< 1000 บรรทัด) → Monolithic
│  ├─ กลาง (1000-10000 บรรทัด) → Layered
│  └─ ใหญ่ (> 10000 บรรทัด) → Layered
│
├─ ระยะเวลาพัฒนา?
│  ├─ ต้องการเร็ว (< 1 เดือน) → Monolithic (สำหรับ MVP)
│  └─ มีเวลา (> 1 เดือน) → Layered
│
└─ ต้องการ maintainability สูง?
   ├─ ใช่ → Layered
   └─ ไม่ → Monolithic
```
**อธิบายเหตุผลของการตัดสินใจแต่ละข้อ:**

- **ขนาดทีม:** ทีมใหญ่ต้องการโครงสร้างที่ชัดเจนเพื่อทำงานร่วมกันได้อย่างมีประสิทธิภาพ **Layered Architecture** ช่วยลด Conflict ในการแก้ไขโค้ด
- **ขนาดโปรเจกต์:** โปรเจกต์เล็กๆ ไม่ได้ประโยชน์จากความซับซ้อนของ **Layered** แต่เมื่อโปรเจกต์ใหญ่ขึ้น การแบ่ง Layer จะช่วยจัดการความซับซ้อนได้ดีกว่า
- **ระยะเวลาพัฒนา:** **Monolithic** เริ่มต้นได้เร็วกว่า เหมาะสำหรับการทำ Prototype หรือ MVP แต่ถ้ามีเวลา การลงทุนใน **Layered** จะให้ผลตอบแทนที่ดีในระยะยาว
- **Maintainability:** นี่คือจุดแข็งที่สุดของ **Layered Architecture** ถ้าต้องการโปรเจก-ต์ที่ดูแลรักษาง่ายในอนาคต **Layered** คือคำตอบที่เหมาะสมที่สุด
